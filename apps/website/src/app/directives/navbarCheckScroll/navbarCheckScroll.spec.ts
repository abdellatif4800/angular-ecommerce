import { NavbarCheckScroll } from './navbarCheckScroll';

describe('NavbarCheckScroll', () => {
  it('should create an instance', () => {
    const directive = new NavbarCheckScroll();
    expect(directive).toBeTruthy();
  });
});
